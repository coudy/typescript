var e = function (x, y) {
    return x.length;
};
var r99 = map(e);

var e2 = function (x, y) {
    return x.length;
};
var r100 = map2(e2); // should be {}[] for S since it is immediately fixed with 0 candidates
